_export:
  !include : 'input_params.yaml'

+cleanup_temp_tables:
  loop>: ${temporary_tables}
  _do:
    +drop_table_${item}:
      td>: SELECT 1
      query: |
        DROP TABLE IF EXISTS ${sink_database}.${item}
